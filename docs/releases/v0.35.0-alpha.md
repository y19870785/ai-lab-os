# v0.35.0 Alpha 发布说明——Local Daily Operating Loop

## 1. 发布身份

- 版本：`0.35.0`
- 名称：`v0.35.0 Alpha — Local Daily Operating Loop`
- 类型：GitHub Pre-release 候选；不发布 wheel 或 sdist 附件
- 当前状态：`RELEASE_CANDIDATE_VALIDATED / DRAFT_PR_OPEN / PENDING_INDEPENDENT_REVIEW / NOT_PUBLISHED`
- 授权边界：`v0.35.0` Tag 与 GitHub Release 均为 `NOT_AUTHORIZED / NOT_CREATED`

## 2. 发布亮点

本版本汇总 SP-016～SP-020 已完成并通过正式验收的本地日常工作闭环：Waiting-For、
Work Log、Daily Review、Local Daily Profile、确定性 Action Hints、Review-to-Action，
以及受控 shutdown、restart 和静止备份/恢复。REL-035 只做发布收口，不新增产品行为。

## 3. 已验证能力

- Canonical Waiting-For Domain 与 Inbox confirmation（ACC-016、ACC-017）。
- Canonical Work Log query boundary（ACC-018）。
- `today` / `yesterday` Daily Review read model（ACC-019）。
- Windows Local Daily Profile、正式 Daily Review CLI、Action Hints 与 Review-to-Action（ACC-020）。
- Scheduler 持续运行、优雅和重复 shutdown、restart recovery、静止备份与隔离恢复（ACC-020）。

## 4. 支持入口

- CLI：`python -m cli profile --require-local-daily`、`python -m cli daily-review --date today`、
  `python -m cli daily-review --date yesterday --json`、Agenda、Inbox 与 Waiting-For 命令。
- API：`/health/live`、`/health/ready`、`/daily-review`、`/daily-review/action-hints`、
  `/agenda`、Inbox 与 Review-to-Action 路由。
- CEO Assistant：今日/昨日简报与既有 canonical service 边界。

## 5. 从 v0.34.0 升级

1. 停止 v0.34.0 进程并确认所有数据库连接已释放。
2. 完整备份 checkout 外的整个 data directory；不得复制运行中的 SQLite 数据目录。
3. 切换或安装 v0.35.0 release candidate。
4. 从 `config/local-daily.env.example` 对照现有配置，继续使用稳定绝对
   `AI_LAB_DATA_DIR` / `AI_LAB_SQLITE_DIR`，并核对 timezone、feature flags、
   API token 与完整 WorkspaceKey。
   `AI_LAB_PROVIDER_MODE` 必须显式配置；`AI_LAB_SQLITE_DIR` 必须是绝对路径且位于
   `AI_LAB_DATA_DIR` 内；`AI_LAB_TIMEZONE` 必须是有效 IANA timezone；所有必要
   Local Daily feature flags 必须显式配置。
5. 先执行 `python -m cli profile --require-local-daily`，再启动服务并完成 health、auth、
   Daily Review、Agenda、Inbox/Waiting-For、Action Hints 与 Review-to-Action smoke。
6. 若验证失败，停止候选版本并从完整备份恢复；不要修改原数据副本继续试验。

## 6. 数据兼容性

数据兼容结论：No destructive database migration is required.

既有表结论：No existing v0.34.0 table rewrite is required.

旧数据处理：No legacy data import is required. No dual-write migration is required.

既有 UserTask、Reminder、Inbox 与 Work Log 保持原样。若 `followups.db` 不存在，
`waiting_for_items`、`waiting_for_events` 及相关索引只通过 `CREATE TABLE IF NOT EXISTS`
和 `CREATE INDEX IF NOT EXISTS` 增量初始化。升级验证必须在 v0.34.0 数据的隔离副本上进行。

## 7. 安全边界

Local Daily 仅绑定 localhost，并要求非示例 Bearer Token 与显式 CORS allowlist/deny-all。
它仍没有 OAuth、JWT、用户身份、RBAC、强多租户隔离或内建 TLS；不得描述为 production-ready。

## 8. 已知限制与未包含范围

本版本不包含 External Notification、Recurring Reminder、Web UI、完整 Knowledge 主链路、
自动 Tool Calling、完整 MCP/Agent Runtime 产品闭环、Docker 正式发布验证、企业级部署、
高可用或长期压力基线。Daily Review 只支持 `today` / `yesterday`，Action Hints 是确定性子集。

## 9. Release Candidate 验证结果

本候选版本已完成以下验证；这不是 ACC-020 A～V 的重新执行：

| 验证项 | 实际结果 |
|---|---|
| Governance + Version | `37 passed` |
| pytest non-real | `1708 passed / 27 warnings` |
| Full pytest | `1708 passed / 5 skipped / 27 warnings` |
| Ruff | `PASS` |
| v0.34.0 Data Compatibility | `PASS` |
| Local Daily Profile | `PASS` |
| Profile Negative Validation | 原 5 项 `PASS`；新增 4 项 `PASS` |
| Service / CLI / API Smoke | `PASS` |
| Action Hints / Review-to-Action | `PASS` |
| Restart / Backup / Restore / Shutdown | `PASS` |
| Provider Calls | `0` |
| Build | `ai_lab-0.35.0-py3-none-any.whl`；`ai_lab-0.35.0.tar.gz` |
| Twine Metadata | `PASS` |
| Fresh Installation | `PASS` |
| Runtime / Distribution Version | `0.35.0` |
| Pull Request Quality Gate | `SUCCESS` |

原 5 项负向场景为缺少 API token、相对 data path、非 localhost bind、Knowledge 启用和
WorkspaceKey 不完整；均 fail closed。独立审查新增的 4 项使用隔离目录、sentinel API
credential、mock Provider 且清除真实 Provider credentials，结果如下：

新增补测的共同输入配置为：`AI_LAB_PROFILE=local-daily`，data/sqlite 使用隔离绝对路径，
timezone 为 `Asia/Shanghai`，Provider 为 `mock`，UserTask、Daily Review、Reminder、
Scheduler 与 API flags 为 `true`，Knowledge 与 Coordination flags 为 `false`，API auth
启用并使用 sentinel token，bind 为 `127.0.0.1`，WorkspaceKey 五项完整。每个场景只替换
表中列出的输入；缺失项场景会删除对应环境变量。

| 补测输入配置 | 命令 | 退出码 | FailureInfo / 错误摘要 | 意外数据文件 | Provider 调用 |
|---|---|---:|---|---|---:|
| `AI_LAB_SQLITE_DIR` 位于 `AI_LAB_DATA_DIR` 外 | `python -m cli profile --require-local-daily` | 2 | `config.profile_invalid / validation / ValueError`；`Local Daily Profile configuration invalid` | NO | 0 |
| `AI_LAB_TIMEZONE=Invalid/REL035` | `python -m cli profile --require-local-daily` | 2 | `config.profile_invalid / validation / ValueError`；`Local Daily Profile configuration invalid` | NO | 0 |
| 缺失 `AI_LAB_PROVIDER_MODE` | `python -m cli profile --require-local-daily` | 2 | `config.profile_invalid / validation / ValueError`；`Local Daily Profile configuration invalid` | NO | 0 |
| 缺失 `AI_LAB_ENABLE_DAILY_REVIEW` | `python -m cli profile --require-local-daily` | 2 | `config.profile_invalid / validation / ValueError`；`Local Daily Profile configuration invalid` | NO | 0 |

新增补测结论：`Validation Result: PASS / Unexpected Files Created: NO / Provider Calls: 0`。

待完成的独立发布门禁：

```text
Release PR Merge: PENDING
Post-Merge main Quality Gate: PENDING
Post-Merge Reconciliation: PENDING
Release Head: NOT FROZEN
Tag: NOT AUTHORIZED / NOT CREATED
GitHub Release: NOT AUTHORIZED / NOT CREATED
```

## 10. 来源与发布权限

- 实施授权 Base：`e596c3331ed86dbba3aeded3ccd61517d1901559`
- 发布规划：`docs/project/REL-035-V035-ALPHA-RELEASE-PLAN.md`
- 实施合同：`docs/project/REL-035-IMPLEMENTATION-TASK.md`
- 历史验收：ACC-016～ACC-020 均为 `PASSED / FINAL`
- 上一已发布版本：`v0.34.0` GitHub Pre-release

Release Candidate 验证、Release PR 合并、Tag Authorization 与 GitHub Release Authorization
是独立治理事件。本文件不创建或授权 Tag，也不发布 GitHub Release。
